# scribe, in this project

<!-- covers: none -->

The role itself is shared: `~/.claude/agents/scribe.md`, from the workflow repository.
This file is the part that is only true here, and it wins where the two disagree.

Documents live at the repository root. Content pages are site source rather than
documentation and are not this role's to edit.

There is no project ownership map, so `~/.claude/doc-owners.json` is the whole of it.
This repository is **public**; its root README is drafted rather than edited.

Translations are checked by `scripts/lint-translations.mjs`. A document change that
touches a translated string is incomplete until that passes.
