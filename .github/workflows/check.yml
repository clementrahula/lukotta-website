# Builds the site and checks the result. Runs on every push and pull request.
# This is the signal that a change is sound; deploy.yml is what publishes it.

name: Check

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Build
        run: node scripts/build.mjs
      - name: Check
        run: node scripts/check.mjs
